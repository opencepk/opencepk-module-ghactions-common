{
  "name": "github-flow-sync-with-mirror",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest ../../jest.config.mjs",
    "build": "rm -rf dist && ncc build index.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^4.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
